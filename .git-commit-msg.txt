fix(frontend): corregir healthcheck del contenedor y completar coleccion Postman

- El HEALTHCHECK del frontend usaba "wget http://localhost:80"; dentro del
  contenedor "localhost" resuelve primero a IPv6 (::1) y nginx solo escucha
  en IPv4 ("listen 80;"), por lo que el contenedor quedaba marcado
  "unhealthy" pese a funcionar correctamente. Se cambia a 127.0.0.1 explicito
  (mismo ajuste preventivo en el healthcheck del backend dentro del
  docker-compose.yml de "04. Entregables").
- Se agrega a la coleccion Postman el caso "Debit another owner's wallet
  with customer token (expect 403)", validado en vivo contra el backend.
