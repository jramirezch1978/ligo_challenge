# Base URL de la API. Dejar vacio para usar rutas relativas (/api/...), que es
# lo correcto cuando el frontend se sirve detras del proxy de nginx (ver
# nginx.conf y deploy.bat) o durante desarrollo con el proxy de Vite.
VITE_API_BASE_URL=
